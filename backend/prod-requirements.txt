# Django Framework & API
Django==3.2.24  # עדכון לגרסת LTS האחרונה
djangorestframework==3.14.0

# Database & Environment Variables
psycopg2-binary==2.9.9
django-environ==0.11.2

# Reverse Proxy & Performance
gunicorn==21.2.0
whitenoise==6.6.0  # ניהול קבצים סטטיים ב-Prod

# CORS & Security
django-cors-headers==3.14.0
djangorestframework-simplejwt==5.3.0  # Auth מבוסס JWT
django-allauth==0.60.1  # ניהול התחברות (Google, Facebook, GitHub)

# Caching & Background Tasks
redis==5.0.1  # חיבור ל-Redis (Cache / Queues)
celery==5.3.4  # משימות רקע
django-cacheops==7.0.0  # ביצועים גבוהים עם Redis

# Monitoring & Logging
prometheus-client==0.18.0  # ניטור Metrics
sentry-sdk==1.39.1  # ניהול שגיאות עם Sentry
django-prometheus==2.2.0  # ניטור Django

# Security & Compliance
bandit==1.7.5  # בדיקות אבטחה בקוד
safety==2.3.5  # בדיקות חולשות אבטחה בתלויות
